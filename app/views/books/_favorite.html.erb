<% if book.favorited_by?(current_user) %>
  <td>︎<%= link_to "♥#{book.favorites.count}", book_favorites_path(book), class: "text-danger", method: :delete, remote: true %></td>
<% else %>
  <td><%= link_to "♥#{book.favorites.count}", book_favorites_path(book), class: "text-blue", method: :post, remote: true %></td>
<% end %>